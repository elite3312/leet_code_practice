# c++ notes

## STL

```cpp
//vector
vector<int>
vector<vector<int>>arr(n, vetor<int>(m, 0))
n * m
vector<Node*>
arr.resize(n, vector<int>(m, 0))

//map(0(n) access, using red balck tree)
map<int, int> m[n] = 1;

//set(0(n) access, using red balck tree)
set<int> s.insert() s.find()!=s.end() s.count()

//queue
queue<int> q.push() q.front() q.pop()

//stack
stack<int> s.push() s.pop()

//haspmap(o(1) access)
unordered_map<int,int>

//set (o(1) access)
unordered_set<int>

//priority_queue
priority_queue<int>
```
